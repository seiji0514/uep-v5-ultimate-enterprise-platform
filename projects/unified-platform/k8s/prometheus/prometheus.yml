# Phase 2-3: Prometheus - scrape unified app metrics + alerts
global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: "unified-platform"
    static_configs:
      - targets: ["app:8000", "app1:8000", "app2:8000"]
    metrics_path: /metrics
