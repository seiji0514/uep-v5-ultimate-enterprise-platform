apiVersion: v1
kind: Secret
metadata:
  name: uep-secrets
  namespace: uep-v5
type: Opaque
stringData:
  SECRET_KEY: "CHANGE_THIS_IN_PRODUCTION"
  DATABASE_URL: "postgresql://postgres:postgres@postgres-service:5432/uep_db"
  REDIS_URL: "redis://redis-service:6379/0"
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
  VAULT_TOKEN: "CHANGE_THIS_IN_PRODUCTION"

---
apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-secret
  namespace: uep-v5
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: # Base64 encoded docker config
