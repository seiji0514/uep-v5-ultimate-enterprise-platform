# Kubernetes デプロイメントガイド

**UEP v5.0 - Kubernetesマニフェスト**

---

## 📋 概要

このディレクトリには、UEP v5.0をKubernetesクラスターにデプロイするためのマニフェストファイルが含まれています。

---

## 🚀 デプロイ手順

### 1. 名前空間の作成

```bash
kubectl apply -f namespace.yaml
```

### 2. 設定とシークレットの作成

```bash
# ConfigMapの作成
kubectl apply -f configmap.yaml

# Secretsの作成（値を適切に設定してください）
kubectl apply -f secrets.yaml
```

### 3. データベースのデプロイ

```bash
kubectl apply -f postgres-deployment.yaml
```

### 4. Redisのデプロイ

```bash
kubectl apply -f redis-deployment.yaml
```

### 5. バックエンドのデプロイ

```bash
kubectl apply -f backend-deployment.yaml
```

### 6. Ingressの設定

```bash
kubectl apply -f ingress.yaml
```

### 7. オートスケーリングの設定

```bash
kubectl apply -f hpa.yaml
```

---

## 🔧 設定

### 環境変数

`configmap.yaml`と`secrets.yaml`で環境変数を設定します。

### リソース制限

各デプロイメントでリソースのリクエストと制限を設定しています。

---

## 📊 モニタリング

Prometheusのメトリクスは自動的に収集されます。

---

## 🔒 セキュリティ

- Secretsを使用した機密情報の管理
- TLS証明書の設定（cert-manager使用）
- ネットワークポリシーの適用（推奨）

---

以上
